# 诊断知识库流水线 · 调度全流程与经验总结

> 面向"真正的全量处理"（583 篇文档 + 4200+ 图，2.6GB）沉淀。上一波（19 篇产品文档 +
> 更早 10 篇 FAQ）跑通了完整流程并踩平了一系列坑，本文把**流程**和**经验**固化下来，
> 供本次及后续大批次直接复用。

---

## 一、端到端流程（一批文档从 zip 到可上传产物）

```
zip → 解压到 input/（UTF-8 名、剔 __MACOSX/.DS_Store）
  → ragkb run [--force]：
      发现文档(递归) → 每篇并行(doc pool):
        parse(建标题树+归并图片OCR块)
        → vision 读图(多模型并行, >5MB/GIF 预处理, 缓存)
        → classify(prefilter 确定性 + LLM 兜底)
        → extract QA / extract SOP(按 section, 缓存, 截断/无效JSON 加大token重试)
        → Layer1 结构门(截断/空/断fence)
        → qa→sop fallback(判qa却抽空的 section 回退为 SOP, 发前端可见事件)
        → coverage 断言(每个非skip section 必产出有效单元)
      → _consolidate(跨文档): aggregate → 并发审核(QA+SOP) → 一次source-grounded重生成 → dedup
      → 增量落盘 results.json(consolidate前+finally兜底) + manifest
  → ragkb export：results.json 重放 → qa_pairs.csv + sop/*.md + metadata.jsonl + zip
  → (可选)语义合并：scripts/merge_sop_semantic.py 把碎 SOP 按语义主题合成少量文件
  → Upload/：SOP文档/ + QA问答/ 归集，人工上传两个口
```

**关键命令**
- `ragkb run` 全量；`ragkb run --only <topic> [--only ...]` 单/多篇；`--force` 忽略幂等跳过；`--no-export` 只处理。
- `ragkb export` 从 results.json 重放导出（不重烧模型）。
- `ragkb serve --port 8137` 仪表盘（实时进度/并发/事件）。

---

## 二、可靠性设计（本波逐个踩坑修出来的，全量必须依赖这些）

1. **内容寻址缓存 `output/.cache/`（断点续跑的命根）**
   - vision/extract/sop 成功即落盘、原子写(tmp+replace)；配额耗尽/截断/失败**不写缓存**，重跑自动重试。
   - 全量中途因任何原因(限流/配额/崩溃/手动停)中断 → **重跑同一命令即从断点续**，已成功的图和抽取秒过。**别删 .cache/**。

2. **增量落盘（真 bug 修复，最高优）**
   - `_persist_results()` 在 `_consolidate()` **之前先落一次**(提交抽取快照) + `finally` **再兜底落一次**(即使 consolidate 抛异常也不丢已 approved)。
   - 不变量：**任何路径退出 run()，已 approved 单元必在 results.json**。全量跑几小时，这条保证"跑完过的成品一定保留"。

3. **多模型并行调度（吞吐命脉）**
   - 网关限流(429 code:2003)**per-model 独立**（探针 `scripts/probe_rate_limit.py` 实证）；配额(429 code:2001)也 per-model。
   - `_PerModelWindows`：每模型独立 AIMD 窗口；`_with_model_fallback` **组内最空闲(headroom)分发** → 聚合吞吐≈各模型之和。
   - 某模型 429→其窗口收缩自然少派活；配额耗尽→剔除该模型其余顶上；全组耗尽才 `LLMQuotaError`。
   - `.env`：`JD_LLM_FALLBACK_MODELS` = 五模型组(Gemini-3.1-Pro/Flash + Sonnet-4.6 + Opus-4.6/4.7，都多模态)；`RAGKB_MAX_WORKERS=10`。

4. **审核并发（曾是最大瓶颈）**
   - `review_qa`/`review_sop` 原来批次**严格串行**(单并发)，把审核阶段卡死。已改 `ThreadPoolExecutor(max_workers)` 并发跑批次 + 并发 repair；限流由多模型窗口承接。
   - 配额耗尽仍优雅降级：保留已审结论 + 其余标 needs_review，**不丢抽取**。

5. **幂等/pin**
   - manifest 记 source_sha + pipeline_version；未变的 done 文档重跑自动 `SKIPPED`。
   - pin 的文档重跑绝不覆盖。**注意脏 pin**：曾有文档 `running` 时被 pin，从未成功产出却被跳过——全量前先查 manifest 有无 `pinned=True 但 extracted=0` 的假 pin。

---

## 三、踩过的坑（全量务必预防）

| 坑 | 现象 | 根因 | 已修/规避 |
|---|---|---|---|
| **限流 vs 配额混淆** | 满屏 429 | code:2003=限流(自动重试,不用管)；code:2001=配额耗尽(切模型/停) | 监控只盯 2001，2003 是噪音 |
| **分类误判→抽空→整篇失败** | `coverage gap` | 产品动态/发布公告被判 qa 却抽不出问答 | prompt 收紧 + prefilter 日期/版本号归 sop + qa→sop fallback |
| **超大 SOP section 失败** | `invalid JSON`/`timed out` | 5000+字+十几图，输出超 token/超时 | extract_sop 加大 token(12288)重试；**极端大 section 需临时提高 `JD_LLM_TIMEOUT`**(曾用 240) |
| **GIF/大图 >5MB 网关 400** | `400 Bad Request` | 网关图片 5MB 上限；17-18MB 动图 | `imageprep.fit_image()` 送图前降尺寸/GIF取首帧(需 Pillow) |
| **审核 pass 却无产出** | results 无更新 | consolidate 全成功才落盘一次 | 增量落盘(见上) |
| **前端进度回退** | 仪表盘倒退/last_run丢 | 并发心跳挤爆事件窗口 | /api/state 扫全量 journal + 心跳与状态事件分离 |
| **跨进程前端不实时** | CLI 跑时看板不动 | CLI 与 Web 各一个内存 bus | 磁盘 events.jsonl 事件流，两进程共享 |
| **failed_review 沉默丢弃** | 部分单元不产出 | fail-closed 正常，但 no_verdict 可能是限流丢判定 | 跑完复审：revise→重生成，no_verdict→干净重审；仍不过才丢 |

---

## 四、全量作战手册（本次 583 篇专用）

1. **规模预期**：583 篇 ≈ 上批 30 倍。图 4200+。**几乎必然中途配额耗尽**——这是**正常停止信号**，不是失败。停了就重跑同一命令续。
2. **重合免跑**：本批含与上批同名模块(产品动态/产品介绍/使用手册/创建或加入工作空间/基本概念)。靠 **manifest 幂等 + 缓存**避免重复：若 source_sha 相同则自动 SKIP；不同(内容更全)则重跑。**不要手动删已处理的**，让幂等机制判。
3. **分批思路**：不必一次 583 篇。可 `--only` 按来源(来源01~10/培训材料/白皮书…)分批跑，每批跑完落盘、可上传，降低单次风险。
4. **监控**：`ragkb serve` 看总进度条+多模型并发 gauge；后台 `tail -f` 日志，只盯 `code:2001`(配额)、`✗`(文档失败)、`coverage gap`、`导出:`。2003/congestion 是噪音。
5. **收尾**：全量 run 完 → 复审 failed_review(scripts/rehabilitate_failed_sop.py)→ 补缺失模块 → 语义合并(merge_sop_semantic.py)→ 归集 Upload/。
6. **别做的事**：不删 .cache/；不因 2003 限流恐慌停；不手动改 results.json(改在可重放的源，用 export 重导)；改代码需重启才生效(改前先让在跑的落盘)。

---

## 五、关键文件速查
- 编排/落盘/幂等：`src/ragkb/pipeline/orchestrator.py`
- 多模型调度：`src/ragkb/llm/client.py`(`_PerModelWindows`/`_with_model_fallback`)
- 分类：`classify.py` + `prompts.py::CLASSIFY_SYSTEM`
- 抽取(重试)：`extract.py::extract_sop`
- 审核(并发)：`gate_semantic.py`
- 图预处理：`imageprep.py`
- 语义合并：`scripts/merge_sop_semantic.py`
- 限流探针：`scripts/probe_rate_limit.py`
- 复审/补救：`scripts/rehabilitate_failed_sop.py`、`retry_no_verdict_sop.py`
